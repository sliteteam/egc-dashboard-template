{
  "name": "egc-dashboard-template",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "refresh:example": "node scripts/fetch-data.example.mjs > data.json && npm run build",
    "validate": "npm run build && node --check scripts/build.mjs"
  },
  "license": "MIT"
}
